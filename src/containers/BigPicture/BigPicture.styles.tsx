import styled from 'styled-components';

const BigPictureStyled = styled.img`
  height: 40vh;
  width: 40vw;
  box-shadow: 1rem .5rem .5rem black;
  margin-top: 10rem;
  margin-bottom: 5rem;
`;

export default BigPictureStyled;
